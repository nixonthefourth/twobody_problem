\section{Problem Setup}

Assume $G=1$,

The Sun: $m = 1000$ at position $\vec{r} = (0,0) $

The Planet: $m$ is irrelevant, since it cancels out in the two-body derivation of Newtonian Gravitational Attraction. And the position is $\vec{r} = (100, 75)$.

The objects are placed at a distance $r$.

\includegraphics[width=0.7\textwidth]{figures/problem_setup.png}